---
title:  Blog archive
filter: haml
---

%h1 Blog archive

- sorted_articles.each do |article|
  %h2= link_to article[:title], url_for(article)
  %p.stamp= article[:created_at]

  %p= article[:excerpt]
